{
  "name": "@hypericon/utils",
  "version": "0.2.1",
  "description": "Utility and formatting functions with no dependencies",
  "author": "Ed Johnson, Hypericon Ltd.",
  "license": "MIT",
  "main": "dist/utils.js",
  "scripts": {
    "dev": "rimraf dist && tsc -w --preserveWatchOutput",
    "example": "node dist/example/example.js",
    "prebuild": "rimraf dist",
    "build": "tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run test && npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^20.2.3",
    "jest": "^29.5.0",
    "rimraf": "^5.0.1",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "files": [
    "dist"
  ]
}
